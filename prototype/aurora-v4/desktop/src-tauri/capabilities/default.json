{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the Aurora v4 prototype window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
